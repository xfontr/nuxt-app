export type Translations = {
    keyboard: {
        space: string;
        left: string;
        up: string;
        right: string;
    };
    linterRay: string;
    instructions: {
        start: string;
    };
};
